{
  "name": "maven-packages",
  "version": "1.0.0",
  "description": "This is repo is for storing all maven packages - updated shouldn't trigger a pipeline",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ua-parser-js": "^0.7.30"
  }
}
